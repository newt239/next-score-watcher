import { extendBaseTheme } from "@chakra-ui/react";
import { Button, Checkbox } from "@chakra-ui/theme/components";

const theme = extendBaseTheme({
  components: {
    Button,
    Checkbox,
  },
});

export default theme;
